import {
  formatDate,
  formatTime,
  formatRelativeDate,
  formatMonth,
  formatWeekday
} from '@commitment/localization';

export const dateFormatter = {
  formatDate,
  formatTime,
  formatRelative: formatRelativeDate,
  formatMonth,
  formatWeekday,
};
