# @commitment/api-contracts

Shared schemas and HTTP API response/request contracts for the monorepo.

## Installation

Add to package dependencies:

```json
"dependencies": {
  "@commitment/api-contracts": "workspace:*"
}
```
