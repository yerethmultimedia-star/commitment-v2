# @commitment/shared

Common agnostic helper types, contracts, and software structures.

## Installation

Add to dependencies in `package.json`:

```json
"dependencies": {
  "@commitment/shared": "workspace:*"
}
```
