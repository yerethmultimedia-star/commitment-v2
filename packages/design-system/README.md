# @commitment/design-system

Shared design tokens (colors, curves, spacing, typography) for the Commitment v2 application suite.

## Installation

Add to package dependencies:

```json
"dependencies": {
  "@commitment/design-system": "workspace:*"
}
```
